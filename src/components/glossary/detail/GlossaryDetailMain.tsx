import {Container} from "@mui/material";
import {useState} from "react";

export default function GlossaryDetailMain() {
    const [mode, setMode] = useState('create');
    return (
        <Container>
            <h1>Detail A</h1>
        </Container>
    );
}